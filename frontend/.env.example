# Mapbox access token — required for the map to render.
# Get one from https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=

# Base URL of the backend API. Defaults to http://localhost:8000 if unset.
VITE_API_URL=http://localhost:8000
